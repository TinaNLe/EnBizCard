# Subpath the app is served from. Leave empty for root deployment (default).
# Trailing slash optional — it is normalized at build time.
# Must be set at BUILD time; it is baked into the static bundle.
NUXT_APP_BASE_URL=

# Host port for the production container (default: 8000).
ENBIZCARD_PORT=8000